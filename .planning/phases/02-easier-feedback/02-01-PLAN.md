# Rich cards and explicit negative feedback

1. Enrich the existing catalogue from previously verified source pages with listing-linked photos and whitelisted structured facts. Preserve original IDs and all 533 records. Do not invent photos, copy owner/contact fields, or label cached facts freshly verified.
2. Add explicit per-member dislikes with multiple dropdown reasons, undo and notes. Preserve legacy unlikes as neutral. Export negative preferences and use only supported objective drawbacks for transparent score reductions; never invent visual/safety attributes.
3. Test data parity, escaping, photo URL allowlisting, sync conflicts, member isolation and preference exports. Deploy alongside the password-access change after the user resolves its hosting choice.

Separate decision pending: protecting the page itself requires moving its hosting from GitHub Pages to the user's existing HTTPS serverwheel service. Asked for approval. Do not migrate hosting, weaken access controls or expose the shared password in public source.
