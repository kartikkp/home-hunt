# Remembered shared-password login on GitHub Pages

User decision: retain GitHub Pages and public listing data. Use a server-verified shared password to protect feedback, not the public static catalogue. Preserve existing sessions, per-member feedback, queued changes and database contents.

1. Add a salted server-only password hash, bounded login requests, throttling and signed 180-day sessions. Never store the raw password in source, browser storage or logs. Keep the existing household token compatible.
2. Replace code/pairing controls with password sign-in, a member picker and device sign-out. Preserve pending changes. Publish the previously prepared photo cards, richer details and explicit dislike controls.
3. Run regressions, preserve a private server backup, deploy the application without replacing the database, then push main and verify Pages assets plus live HTTPS login/state access without changing real feedback.
